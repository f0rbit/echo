{
	"name": "bestiary",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"main": "src/main.ts",
	"scripts": {
		"dev": "bun build src/main.ts --outdir dist --watch --target browser",
		"build": "bun build src/main.ts --outdir dist --target browser && cp index.html dist/ && cp -r public/* dist/",
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"record:arena": "bun run tools/record-arena.ts"
	},
	"dependencies": {
		"@f0rbit/corpus": "^0.3.5",
		"@f0rbit/forge": "^0.4.2",
		"pixi.js": "^8",
		"zod": "^3.25.0"
	},
	"devDependencies": {
		"@types/bun": "^1.3.0",
		"typescript": "^5.6.0"
	}
}
